command /menu:
    executable by: players
    aliases: /m
    trigger:
        if player has permission "group.cwadmin":
            make sender execute command "/dm open menu_admin" with permission "deluxemenus.open"
        else:
            make sender execute command "/dm open menu" with permission "deluxemenus.open"